{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Set",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the set of resources."
    },
    "description": {
      "type": "string",
      "description": "Details about the set of resources."
    },
    "version": {
      "type": "string",
      "description": "The version of Saturn Cloud used when creating the set of resources."
    },
    "resources": {
      "type": "array",
      "description": "List of resources to be included in this set.",
      "items": [
        {
          "$ref": "/resources/schema.json"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "resources",
    "version"
  ]
}

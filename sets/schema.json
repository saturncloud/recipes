{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Set",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the set of recipes."
    },
    "description": {
      "type": "string",
      "description": "Details about the set of recipes."
    },
    "schema_version": {
      "type": "string",
      "description": "The version of Saturn Cloud used when creating the set of recipes."
    },
    "resources": {
      "type": "array",
      "description": "List of recipes to be included in this set.",
      "items": [
        {
          "$ref": "/resources/schema.json"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "resources",
    "schema_version"
  ]
}
